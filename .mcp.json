{
  "mcpServers": {
    "autoclaude-memory": {
      "command": "node",
      "args": ["./dist/mcp/index.js"],
      "env": {
        "AUTOCLAUDE_DB": "${HOME}/.autoclaude/memory.db"
      }
    }
  }
}
